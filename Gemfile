# frozen_string_literal: true

source "https://rubygems.org"

gem 'json' # This gem is used to parse JSON data and generate JSON strings.
gem 'securerandom' # This gem is used to generate random UUIDs.
gem 'dry-validation' # This gem is used for data validation.
gem 'zip' # This gem is used for creating and extracting ZIP files.
